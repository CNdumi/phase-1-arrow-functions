function divide() {
    return 2000 / 100;
  }
  
  let x = 2;
  const square = (x) => x * x;
  
  let a = 3;
  let b = 4;
  const add = (x, y) => x + y;
  
  console.log(divide()); 
  
  console.log(square(x)); 
  
  console.log(add(a, b)); 
  